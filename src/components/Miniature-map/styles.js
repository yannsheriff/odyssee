import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    button: {
        position: 'absolute',
        height: 80,
        width: 200,
        bottom: 140,
        left: 150
    },
    svg: {
        position: 'absolute',
        top: 0
    }
})

export default styles
